import com.kms.katalon.core.annotation.AfterTestCase
import com.kms.katalon.core.context.TestCaseContext

class LoginListener
{
	/**
	 * Executes after every test case ends.
	 * @param testCaseContext related information of the executed test case.
	 */
	/*
	 * @AfterTestCase def sampleAfterTestCase(TestCaseContext testCaseContext) {
	 * String tcName = testCaseContext.getTestCaseId()
	 * 
	 * switch (tcName) { case "Test Cases/Login/Login standard user":
	 * CustomKeywords.'saucedemo.logout.doLogout'() break case
	 * "Test Cases/Login/Login problem user":
	 * CustomKeywords.'saucedemo.logout.doLogout'() break case
	 * "Test Cases/Login/Login performance glitch user":
	 * CustomKeywords.'saucedemo.logout.doLogout'() break default: break } }
	 */
}